# Day 3 — follow-along (stub)

Staff execute file. Scope: [`agenda/d3.md`](../agenda/d3.md).
Papers: [`docs/`](../docs/README.md). Brain: **query**, do not rebuild.

**One Night.** Deck not built yet. Do not invent beats. When the Constructor
slides exist, write them here as a Day-1-shaped folder: Show then Hands-On.

Story that night: last Night’s Type `01` Parquet → dlt **registers only** →
Bronze → Silver → Gold → golden-match (two questions, never netted). Same
Bind. Do not reopen ingest leaves that are already green. Type `06` stays sealed.
