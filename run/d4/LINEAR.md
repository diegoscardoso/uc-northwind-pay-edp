# Linear tonight

Linear is **Pass 6 Register’s board**. It is not the eval. It is not
GitKraken. It is not a failed night if the cloud app is dark.

## What you show

Open [`linear-board.md`](linear-board.md) on the projector (or
`docs/tasks/_linear-board.md` in the wrktr-e2e checkout). One row per
leaf. Say:

> The board moves when a packet exists. It does not move when the agent
> says done.

## What you do not do

- Do not run `gk auth login` on stage.
- Do not debug Linear MCP / GitKraken sign-in.
- Do not invent a second queue in Linear mid-night.
- Do not stall tile 04 waiting for a browser.

## If someone asks “where is Linear?”

> Same cards. This file is the board because the cloud connection is
> down. The judge is still the eval + packet.

After class: `gk auth login`, then `gk provider add linear`, then copy
rows if you want them in the Linear workspace.
