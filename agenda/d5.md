# Day 5 — Dark Factory

**Seat:** Dark Factory.  
**Converge:** Full Pass 0–8 on a **sealed** Type `06`. Design again, then unattended build.  
**This file** is the brief for the Day 5 PPT, the flywheel, and the large red pill.  
Keep it short on stage. Build slides from the chapter list. Do not invent extra story. Do not put the planted miss on a slide.

Public gate: *a never-seen type is onboarded live. You leave the loop.*  
Rings tonight: **orchestration.** Dark factory. OntoLayer may answer the new schema; it does not skip Capture → Consensus.

**Swing:** Stage (why a factory / why a new type) → Craft (full spine on a drop you have not seen) → Floor (unattended Type `06` + classify the miss) → Debrief (the plant you trusted can be wrong).

**One Night.** There is no morning and no afternoon. Type `06` arrives as a sealed kit this calendar morning; the session is still one Night.

---

## Intent

Days 1–4 proved the **source** can lie and the **default** can lie. Thursday taught **Context · Eval · Loop** and ran Type `05` unattended. Tonight the room proves the **main plant** can lie, and that a factory can find it without a pair-programmer. They leave able to:

1. Receive a never-seen type kit Friday morning. It was not in `spec/` on Thursday.
2. Run the whole spine: Capture → Intent → Structure → Decompose → Consensus → Tasking → Bind → Loop.
3. Build Type `06` the same way as `01`–`05`: five-file package → landing Parquet → dlt → lakehouse → Gold → golden-match.
4. When the cent disagrees, classify. The honest name tonight may be `CONFIRMED_LEGACY_DEFECT`.
5. Stall the type. Write the evidence. **Do not** edit frozen `legacy/` to go green.

They do not repair the source. They do not patch Java or a procedure. They do not treat model narration as settlement. They do not claim the detector folder is the product — the flywheel is the process they just ran.

---

## Stage — chapters for the PPT

Use this order. One idea per slide. Stop before the miss.

### 1. Why a factory

- A second plant that only works when you watch is a demo.
- Dark Factory here means: new drop in, classified evidence out, lights out in the middle.
- [`plans/dark-factory.md`](../plans/dark-factory.md) is the seed. Tonight we write the next page by running it.

### 2. The spine again

Days 1–2 were Capture → Consensus on a pack written in advance (brain + graph, then ADRs, then the sign). Tonight is Pass 0–8 on a pack the room has not unpacked. Same kits. Same barrier. Same Bind. New type. OntoLayer may answer the new schema; it does not skip Consensus.

### 3. What "never-seen" means

- Type `06` is not in `spec/` until tonight's drop.
- Do not search git history for a prior modern implementation.
- The inbound will look like the others: messy on purpose. The contract still wins.

### 4. The three pills (names only)

| | Source lie (`01`–`05`) | Type `05` small pill | Today (Type `06`) |
|---|---|---|---|
| Who can be wrong | The source declaration | The trusted default | The **legacy plant** |
| Classification | `CONFIRMED_SOURCE_DEFECT` | `MODERN_DEFECT` if copied | `CONFIRMED_LEGACY_DEFECT` |
| Repair | Never | Never | Never |

Do not show the Type `06` number on this slide. The floor finds it.

### 5. Find, classify, stall

```text
golden-match
  → CONFIRMED_LEGACY_DEFECT
  → evidence packet
  → type stalls
```

The factory's job is the classification and the stall. It is not a silent rewrite of Java, of `expected/`, or of the contract.

### 6. What done means tonight

- Type `06` has a signed descent and a bound loop.
- Admissible paths match or are classified.
- The miss is named, evidenced, and not patched.
- The seed in `plans/dark-factory.md` can be enhanced with what the room actually learned. The detector folder is still later.

---

## Craft — kits on this Night

| Kit | Job tonight |
|---|---|
| **Converge** | Full 0–8. Fast, not sloppy. Consensus still signs. |
| **Seamwise** | Same three seams. Type `06` is a new numbered package, not a new estate. |
| **Task-Spec** | Evals include the classification, not only "it loaded." |
| **Brief-Spec** | First hours are explore + decide. After the sign: implement + review. The pill hour is decide. |

### Agent rules tonight

| Rule | Why |
|---|---|
| Unattended after Consensus | That is the factory |
| Bind still fences `legacy/`, `contracts/`, `gen/`, `infra/` | The pill is unpatchable by construction |
| Narration is not evidence | Settlement is the eval + the classification |
| Do not weaken golden-match | Friday is why the referee has `CONFIRMED_LEGACY_DEFECT` at all |

---

## Floor — live work (instructor + tables)

1. Drop the Type `06` kit into `spec/` (or the Night's share). No preamble about the miss.
2. Tables run Pass 0–4 on a clock. Owner signs. Same barrier as Day 2. The week notebook ([`brain/notebooklm/`](../brain/notebooklm/README.md)) covers types `01`–`05` only — Type `06` is a **new** source tonight, not in the zip. OntoLayer may answer the new schema. Neither replaces the sign.
3. Task, bind, leave. Watch the eval, not the keystrokes.
4. When golden-match returns a difference: **do not speak the answer.** Ask the two questions out loud.
5. If modern matches the contract and legacy does not: `CONFIRMED_LEGACY_DEFECT`. Write it down. Stall.
6. Show the frozen fence: the agent cannot "just fix Java." That is the point.
7. Debrief only then: enhance the factory seed with the evidence path you actually produced.

If modern is also wrong, that is `MODERN_DEFECT` — fix the new plant and re-run. Do not use that as a way to avoid the legacy classification if both facts are true; classify what the evidence says, one code at a time.

---

## Debrief

- The source can lie. The default can lie. The main system can lie.
- Green is not the goal. Green for the right reason is.
- You did not migrate NorthWind Pay. You built a second plant that can indict the first.

---

## Homework

None that continues the factory tonight. The week is closed when the Type `06` packet and the classification are on disk.

Optional, not extra credit: one paragraph in the factory seed — what the flywheel did, what it must not do.

---

## Research

The week closes tonight. Nothing new to query.

Optional: one paragraph in the factory seed — what the flywheel did, and
what it must not do.

---

## Exit criteria

- [ ] Type `06` inbound arrived tonight, not before.
- [ ] Full Pass 0–8 completed. Consensus signed before the loop.
- [ ] Type `06` uses the same modern shape: landing Parquet, not SFTP; dlt register-only; Gold blocked on unresolved.
- [ ] The numeric miss is classified. If the legacy plant is the one that is wrong, the code is `CONFIRMED_LEGACY_DEFECT`.
- [ ] Frozen `legacy/` and `contracts/` were not edited to manufacture agreement.
- [ ] Evidence packet exists. The type is stalled, not "fixed in place."

## Day 5 is not

- Not a sixth day of Type `05`.
- Not a license to patch Java.
- Not a pre-built `factory/` detector as the deliverable.
- Not CI, cloud, or a production claim.
- Not a reveal slide that spoils the miss before the loop runs.
